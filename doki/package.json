{
  "name": "doki-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "DOKI side-channel runtime for FalsifyMe: read-only observation, deterministic history/Q-learning, model selection and SQLite persistence.",
  "type": "module",
  "engines": { "node": ">=22.5.0" },
  "scripts": { "test": "node --test tests/*.test.mjs", "start": "node src/cli.mjs" },
  "license": "AGPL-3.0-only"
}
